# New Resource Checklist

> **NEVER modify generated files directly.** Fix issues in `generator.yaml` or hook templates, then regenerate. Files marked `// Code generated by ack-generate. DO NOT EDIT.` are overwritten every time code generation runs.

## How ACK Infers Resources

ACK looks for AWS API operations starting with `Create`. The singular noun after "Create" becomes the resource name (e.g., `CreateBucket` → `Bucket`). **Spec** fields come from the Create input shape; **Status** fields come from the Create output shape (excluding fields also in input).

**Important scope restriction:** ACK only supports *control plane* resources. Data plane operations (S3 PutObject/GetObject, DynamoDB PutItem/GetItem, SQS SendMessage, Lambda Invoke, Kinesis PutRecord, etc.) are out of scope.

## Feasibility Check

**Standalone CRD — ALL must be true:**
1. Has `Create<Thing>` (NOT `Put<Thing>`)
2. Has `Delete<Thing>` (independent lifecycle)
3. Has its own identity (ARN, Name/ID)
4. Has meaningful independent state

**Field on parent — ANY is true:**
1. Uses `Put<Thing>`/`Get<Thing>` instead of `Create<Thing>`
2. Has `Create<Thing>` but no independent identity
3. Is a configuration toggle on a parent

**Not supported:** Data plane ops, no CRUD API, read-only

## API Investigation Checklist

For each CRUD operation (`api_op_*.go`), note:
- Required vs optional fields on Create
- Spec fields (Create input) vs Status fields (Create output only)
- Whether Update uses same field names as Create (renames needed?)
- Extra fields from Describe (need `from.operation` + `is_read_only`)
- State machine enums → `synced.when`
- Tag support → `tags.ignore: true` if none
- Non-standard patterns (parent ARN, composite keys, non-standard 404)

## Configuration Decision Table

| Question | If Yes |
|----------|--------|
| Resource in `ignore.resource_names`? | Remove it |
| Field names differ across operations? | Add `renames` for EACH operation |
| Async lifecycle states? | Add `synced.when` |
| Update rejected while not ACTIVE (async update API)? | Add `updateable.when`; `deletable.when` for deletes |
| Update API rejects unchanged fields being re-sent? | Add `update_operation.omit_unchanged_fields: true` |
| No tag support? | Add `tags.ignore: true` |
| Fields only in Describe output? | Add `from.operation` + `is_read_only: true` |
| Depends on parent resource? | Add `references` |
| Cross-service reference? | Add `references.service_name` |
| Update needs custom logic | Add `update_operation.custom_method_name` |
| Server sets defaults? | Add `late_initialize` with `skip_incomplete_check: {}` |
| K8s-irrelevant Create input fields? | Add to `ignore.field_paths` |
| Non-standard 404? | Add `exceptions.errors.404.code` |
| Terminal errors? | Add `exceptions.terminal_codes` |
| Immutable fields? | Mark `is_immutable: true` |
| Fields to show in kubectl get? | Add `print.name` |
| Periodic re-sync needed? | Add `reconcile.requeue_on_success_seconds` |

## Generated vs Custom Code

~95% of each controller is generated. Files marked `// Code generated by ack-generate. DO NOT EDIT.` must not be manually edited.

**Generated files** (per resource in `pkg/resource/<name>/`):
`manager.go`, `manager_factory.go`, `sdk.go`, `resource.go`, `delta.go`, `references.go`, `descriptor.go`, `identifiers.go`, `tags.go`

**Hand-written customization points:**
- `pkg/resource/<name>/hook.go` (or `hooks.go`) — Custom lifecycle logic
- `templates/hooks/<name>/*.go.tpl` — Template snippets injected into generated code at specific lifecycle points (survive regeneration)
- `generator.yaml` (root of each controller) — Controls code generation

## Customization Pattern

When the AWS API has quirks not handled by default generation:
1. Define a `custom_method_name` in `generator.yaml` under `update_operation`
2. Implement the custom method in `hook.go`
3. Use template hooks (`.go.tpl` files) to inject code into generated SDK call lifecycle

## Post-Generation Review

After running code generation, verify:
- `apis/v1alpha1/<resource>.go` — Spec/Status split correct?
- `pkg/resource/<resource>/sdk.go` — CRUD mappings correct?
- `pkg/resource/<resource>/delta.go` — comparison logic?
- `cmd/controller/main.go` — blank import exists?
